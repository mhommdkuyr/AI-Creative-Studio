# Experiential Labs gateway
# Create a key in Experiential Labs Settings -> API keys and keep it server-side.
EXPLABS_API_KEY=xpl_your_key_here
EXPLABS_BASE_URL=https://api.experientiallabs.ai/v1
EXPLABS_MODEL=claude-fable-5.1

# Backward-compatible fallback for existing deployments.
# OPENAI_API_KEY=
